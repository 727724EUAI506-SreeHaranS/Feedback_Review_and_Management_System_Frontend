# TODO: Fix AdminDashboard Errors

- [x] Modify `fetchFeedbacks` in `AdminDashboard.jsx` to properly extract the feedback array from the API response object and add error handling for invalid data formats.
